<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{to}}</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
         body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }
        .container {
            max-width: 600px;
            margin: auto;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .header {
            background: #bf1616;
            text-align: center;
            padding: 15px;
            border-radius: 10px 10px 0 0;
        }
        .logo {
            display: block;
            margin: 0 auto;
            max-width: 100px;
            height: auto;
        }
        .content {
            padding: 20px;
            font-size: 16px;
            color: #333;
        }
        .button {
            display: inline-block;
            background: #007bff;
            color: white;
            padding: 10px 15px;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 10px;
        }
        .footer {
            text-align: center;
            padding: 10px;
            font-size: 12px;
            color: #777;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <img class="logo" src="cid:logoHeader" alt="Logo encabezado" />
        </div>
        <div class="content">
            <h1>Recuperacion de contraseña</h1>
            <p>Estimado usuario {{username}}</p>
            <h3>Su nueva contaseña es: {{newPass}}</h3>
            <p>Favor de cambiarla lo antes posible.</p>
        </div>
        <div class="footer">
            <img class="logo" src="cid:logoFooter" alt="Logo pie de página" />
        </div>
    </div>
</body>
</html>
